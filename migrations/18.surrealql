-- Migration 18: FREE tier models ready
-- This migration ensures database is ready for Groq + Gemini
-- Podcast settings remain unchanged (user will configure later)

-- Simple SELECT to validate migration runs successfully
SELECT * FROM schema_version LIMIT 1;
